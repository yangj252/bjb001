APP_PASSWORD=replace-with-a-long-unique-passphrase
# Optional: add more isolated vaults. Format: vault_id=password,another_vault=another-password
# APP_PASSWORDS=guest=change-this-second-password
# Optional advanced override. If omitted, Worker creates a unique 256-bit signing secret in D1.
# COOKIE_SECRET=replace-with-at-least-32-random-characters
